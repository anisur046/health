# Environment Variables for Backend Deployment

# Database Configuration (Optional - defaults to SQLite)
# If you want to use MySQL instead of SQLite, set these:
# MYSQL_HOST=your-mysql-host.com
# MYSQL_USER=your-mysql-user
# MYSQL_PASSWORD=your-mysql-password
# MYSQL_DATABASE=health

# Force SQLite (useful for serverless deployments like Vercel/Render)
FORCE_SQLITE=true

# Node Environment
NODE_ENV=production

# Server Port (automatically set by hosting platforms)
# PORT=3001

# SMTP Configuration (Optional - if missing, Ethereal test account will be used)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# SMTP_FROM="Health App" <no-reply@example.com>
